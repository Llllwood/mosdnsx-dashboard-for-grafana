global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "mosdns"
    # scrape_timeout: 10s
    metrics_path: "/metrics"
    static_configs:
      - targets:
        # 指向mosdnsAPI，自行修改IP、端口
        - 192.168.1.60:15011